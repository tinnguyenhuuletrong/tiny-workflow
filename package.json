{
  "name": "tiny-workflow",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "bun-plugin-dts": "^0.2.3"
  },
  "scripts": {
    "build:npm:all": "bun run --filter 'tiny-workflow*' build:npm"
  }
}